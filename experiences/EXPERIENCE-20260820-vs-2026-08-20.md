---
title: 回滚到历史基线前须检查代码自洽性：调用方参数 vs 函数签名配套（2026-08-20 崩溃事件）
scenario: "git 回滚到凌晨 3 点基线 bd203b5 并重启后，web/飞书桥每次对话抛 TypeError（build.py 向 build_history_messages() 传 tool_tail= 参数，但该函数在 bd203b5 的 history.py 中从不接受此参数）。服务健康检查通过、看着\"活着\"，实际每次流式请求必崩。回滚目标提交代码内部不一致：调用方（build.py 的 tool_tail 传参）与接收方（history.py 的 tool_tail 参数支持，仅存在于被回滚掉的 998fdf2 方案A 提交）不在同一提交内。"
root_cause: 特性参数分两步落地：build.py 调用侧先引入 tool_tail 传参，history.py 参数支持后引入（998fdf2）。回滚到中间提交 bd203b5 后调用侧保留、接收侧缺失，形成悬空参数 TypeError。且 bd203b5 的 config.py 不读 TOOL_TAIL（值恒为 0），健康检查不覆盖对话构建路径，故障静默潜伏到用户真实对话才暴露。
solution: "1) 回滚到历史提交前，先检查目标提交自洽性：git show <commit>:src/... 对比调用方传参与被调函数签名（grep 参数名是否出现在目标提交的函数签名中）；2) 回滚后立即做端到端对话冒烟（真实 /api/v1/chat/stream 请求），不能只依赖健康检查；3) 若已踩坑，最小补丁 = 删除悬空传参（值恒为默认时行为零变化，如 tool_tail 恒 0 属死代码）；若要恢复功能须连参数带调用从 backup 分支一起搬，不能只搬一边；4) 修复后跑相关单测（test_history 等 75 个）+ E2E 验证再收尾。"
evidence: "2026-08-20 00:04-00:11 崩溃事件：guard 自动重启加载 bd203b5 → TypeError；DSH 最小补丁删除 build.py 悬空 tool_tail 传参（留注释），单测 75 绿、E2E 正常返回 done（cache hit 11904/12671）；回滚本体原样保留（HEAD=bd203b5、backup/20260819-after-3am 分支 7 提交、stash@{0} 15 文件、.env=153445 快照）"
tags: [git回滚, TypeError, 代码自洽性, 缓存, tool_tail, E2E冒烟]
source: {}
status: active
created_at: "2026-08-20T00:16:11.052688+08:00"
updated_at: "2026-08-20T00:16:11.052688+08:00"
---