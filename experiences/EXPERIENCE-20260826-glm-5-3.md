---
title: glm-5.3 上下文超限拦截与紧急压缩路径验证：链路正常但归档可检索性未验证
scenario: "压测超大载荷触发上下文超限：glm/glm-5.3 报 908,715 tokens 超 900,000 安全边距，程序守卫拦截+自动紧急压缩（此前 deepseek-v4-flash 路径 912,213 tokens 已验证过同类拦截）"
root_cause: ""
solution: 分层验证结论：①守卫拦截/压缩/恢复链路正常（行为证据：压缩后请求正常进入处理，缓存命中率 97.1% 报告正常）；②「归档可检索」声明未验证通过——后续若复现，优先检查紧急压缩归档写入与索引时机，而非重复换关键词搜索
evidence: architecture_status 回执 success（8069 字符截断摘要，关键事实含 run.compact 路径）；search_archive 4 次空结果回执；event_stream 无匹配回执；本轮请求正常处理即压缩生效的行为证据
tags: [上下文超限, 紧急压缩, run.compact, search_archive, 零丢失验证缺口, 压测]
source:
  model: glm/glm-5.3
  session_context: 压测异常案例重放
status: archived
created_at: "2026-08-26T23:00:05.815315+08:00"
updated_at: "2026-08-26T23:01:40.869721+08:00"
---

观察记录（glm-5.3 路径）：
1. 守卫行为正常：载荷 908,715 tokens 超 glm/glm-5.3 安全边距 900,000，请求未发送（避免必失败调用），自动执行紧急压缩（放弃头部保留、锚点前移归档），下轮请求基于压缩后历史正常发送并正常处理——「拦截→压缩→恢复处理」链路行为证据确凿。
2. 验证缺口（如实声明，不伪造「信息零丢失已验证」）：压缩后 search_archive 以 4 组不同关键词检索（含回执指定的 tool_name=architecture_status 取回方式）均空结果；event_stream 查压缩事件无匹配。可能原因：①紧急压缩（run.compact 锚点前移）归档路径或索引时机与常规压缩不同；②索引延迟未生成；③关键词与归档摘要不匹配。不能断言信息丢失，但「可检索找回」声明在本路径未能验证。
3. 建议：排查 run.compact 紧急压缩分支是否调用与常规压缩相同的归档写入/索引逻辑；若确认未写入，属「拦截成功但零丢失承诺未兑现」缺陷。