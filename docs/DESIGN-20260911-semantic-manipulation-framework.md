# DESIGN-20260911：语义操控框架——以大模型为主体的浏览器/OS 操控架构

> 状态：**DRAFT v0.1**，待用户评审。
> 定位：方案设计（哲学框架 + 契约推导 + 效率机制），不含实现。
> 前置分析：本会话 2026-09-11 对 Anthropic Computer Use / browser-use / Playwright MCP 的技术调研（第三方 token/延迟数字为指示性，未由官方核实）。
> 规则依据：`docs/ai_rules.md`（RULE-AI-00/01/04 原文引用）、`docs/STACK.md` v0.1 草稿（L1/L2/L3 三层权威边界）。

---

## 0. 一句话

把操控的单位从「像素坐标 / 机械无障碍树」升维到「任务语义元素」：**模型在语义层思考与行动，程序负责物理接地、状态维护与诚实回验**。语义是快路径，原始渲染（DOM 片段/截图）是永远可回验的诚实兜底。

---

## 一、哲学框架：五根支柱

### P1 语义是 LLM 的原生介质（token-first）

LLM 生于文本语义流：它的推理、记忆、泛化全部发生在语义空间。它认识「提交订单」这四个字的方式，远比它计算「按钮在屏幕 (347, 612)」的方式自然。

**推论**：操控单位应当等于思考单位。模型思考什么（语义对象），就操控什么（语义元素）；操控什么，就能验证什么（语义动作可回验）。
**违反后果**：像素坐标动作空间强迫模型每步做空间几何推理——这是它的弱项，也是现有截图派方案 flake 率高的结构性原因。

### P2 模型是大脑，程序是感官与手脚

直接推广 RULE-AI-00 原文（`docs/ai_rules.md:13`）：

> 「**程序是 AI 的感官和手脚，非大脑。** 程序提供信息（感官）与执行通道（手脚），不替 AI 决策。」

映射到操控域：

| LFL 语义 | 操控域对应 |
|---|---|
| 感官 | 把物理渲染状态（DOM/树/像素）翻译成任务相关语义事实 |
| 手脚 | 把语义动作翻译成物理操作（坐标、事件、系统调用） |
| 不替 AI 决策 | 不替模型选元素、排序任务、判断页面"哪个重要" |

**违反后果**：程序一旦开始「推荐最佳候选元素」，就在动作层替模型做了策略判断——agency 让渡，且程序对任务一无所知，推荐必然低质。

### P3 语义层只给事实，不给策略

语义表示描述「有什么元素、什么状态、什么关系」；**禁止**出现「先点这个再点那个」「推荐点击」「最佳候选」。

- 程序**可以**做的确定性服务：按结构排序（稳定、可解释）、按模型显式给的语义查询过滤、缓存、坐标解析、等待与断言。
- 程序**不可以**做的：判断元素重要性、推断任务意图、生成操作序列、裁决任务完成。

对应 STACK.md L1 层「不替模型制定策略、不替用户裁决完成」的既定边界。

**违反后果**：语义层塞进策略后，框架自己变成一个小模型——违背「程序是感官非大脑」，且策略错误会被包装成「感知结果」更难被模型发现。

### P4 语义是解释，解释必须可回验（诚实原则）

语义层是框架对物理状态的**解释**，而解释会错、会过时、会漏。因此：

1. **每个语义表示必须绑定接地版本**（DOM 快照 hash / 截图时间戳），模型可显式要求回验原始接地。
2. **盲区如实标注**：canvas/WebGL/动态内容树不可达 → 明确标注「此处语义化盲区」，不返回空集假装「没有元素」。
3. **降级不静默**：语义化组件失败 → `[语义化异常]` 事实+原因+可选动作，循环继续（对应 RULE-AI-04「程序异常如实反馈、不静默、不阻断决策」）。
4. **时效显式**：语义快照是某个时刻的解释，过期与否由模型结合任务判断，程序只标注时间与版本。

**违反后果**：无回验通道的语义层是「善意幻觉放大器」——程序翻译错了，模型在错误解释上推理，还比截图派更自信（因为输入看起来结构化、可信）。

### P5 效率来自分工与增量，不来自压缩

- **分工**：程序做确定性（遍历、坐标、等待、断言、缓存、去重），模型做模糊性（语义匹配、规划、异常解释、恢复策略）。
- **增量**：世界状态不重述，只报 diff；每轮只推进「本步→结果→下一步」（对应方法总则④增量推理）。
- **给对信息而非少给信息**：token 效率的本质是任务相关子集 + 增量变化，不是把信息压到失真。

**违反后果**：全屏视觉语义化把 token 负担加倍赚回来（视觉语义化本身比直接给截图更贵）——语义化必须只花在结构信号（DOM/树）拿不到的地方。

---

## 二、大模型认知剖面 → 操控契约推导

### 2.1 LLM 强项（应放在关键路径上）

| 强项 | 操控域用途 |
|---|---|
| 语义模糊匹配 | 「找到提交订单的按钮」——不需要精确 name |
| 上下文推理与规划 | 多步任务分解、条件分支 |
| 语言泛化 | 不同页面/应用，同语义即同操作动作 |
| 异常解释与恢复 | 「登录态丢了，回去重新登录」 |

### 2.2 LLM 弱项（应从关键路径移走，交给程序）

| 弱项 | 程序接管方式 |
|---|---|
| 精确空间坐标计算 | 程序把语义元素解析为坐标，模型永不碰 x,y |
| 全量信息串行扫描 | 程序裁剪 + 去重 + diff，模型只看任务相关子集 |
| 精确计数与位置记忆 | 程序维护稳定 ID 与结构索引 |
| 长时一致性 | 程序维护快照、版本与水合（回验）通道 |

### 2.3 四个契约面

**① 感知契约——语义元素卡片**

每个可见元素表示为结构化卡片（事实，无策略）：

```
{id: "el_a3f2", role: "button", name: "提交订单",
 state: "enabled", hint: "表单底部右侧，视觉为蓝色主按钮",
 coverage: "dom+tree",        // 接地可信度：哪些感官覆盖了它
 version: "snap_9c1d"}        // 绑定的接地快照版本
```

- `id` 稳定（见②）；`state` 是机械事实（enabled/disabled/checked…）；
- `hint` 只描述**是什么**（位置、外观语义），不描述**该不该点**；
- `coverage` 标注盲区：`"tree-unreachable"` 的元素要么不出现并明示盲区范围，要么标 `coverage: "vision-only"`。

**② 标识契约——稳定语义 ID**

模型全程用 ID 引用元素；程序保证同一物理元素跨轮次 ID 不变（DOM 结构相似性追踪），ID 可随时水合回验到原始接地。这是 LFL EvidenceRef 模式在操控域的同构推广。

**③ 动作契约——语义动词 + 语义对象**

```
click(id="el_a3f2")
fill(id="el_b71", text="...", mode="replace")
select(id="el_c09", value="中国")
scroll(target="semantic:下一屏", scope="region:main")
wait(until="element el_d55 visible" | "url contains /success", timeout_ms=8000)
read(region="form:checkout", depth="raw")     // 三级缩放的第三级：回原始接地
```

- 动作对象永远是 ID 或语义区域，**永远不是坐标**；
- `wait` 是一等动作：等待条件由模型以语义声明，轮询由程序执行——模型不轮询。

**④ 反馈契约——语义 diff + 断言回执**

动作回执 = `[状态] + 什么变了`（新增/消失/状态改变的元素卡片）+ 新快照 version，**不是**新的一整屏。程序可机械验证的（等待达成、URL 变化、元素可见）以断言回执给出；模型只处理程序验证不了的（内容是否正确、任务是否推进）。

---

## 三、信息获取效率的最高形态

原则：**拉为主、推为辅、增量优先、分层缩放、断言优先**。

1. **拉模式主导**：模型按需查询语义视图（`observe(scope, filter)`），程序不主动倾倒整屏。推送仅限会话级重大事件（页面跳转、对话框弹出、崩溃）。
2. **增量 diff**：状态不重述。模型第二轮问「checkout 表单里还有什么没填」，程序只答新增/变化，不重发整个表单。
3. **三级缩放**：概览（页面语义摘要：类型+区域+关键状态）→ 展开（区域/元素组卡片）→ 原始接地（DOM 片段/截图）。模型按需下钻，默认停在能决策的最低层级。同构于 LFL `source_synopsis`（模型自写摘要绑定原文 SHA，程序只存取不生成）。
4. **断言优先**：能验证的交给程序（`wait`/`assert`），把模型从「轮询-观察-判断」循环里解放出来——模型只在断言失败时介入解释。
5. **按需补盲**：vision 只用于树盲区（canvas、动态渲染），且由模型显式请求（`read(depth="raw")` 或 `observe(scope, mode="vision")`），不做全屏视觉语义化。
6. **预算可见**：感知成本（卡片数/token 估算/快照年龄）作为事实维度随工具返回，模型可预判再决定下钻深度（同构 `architecture_status.context_usage` 的赋能模式）。

## 四、交互效率的最高形态

1. **原子默认，组合可选**：单动作原子（可验证、可归因、可回滚）；程序提供批量提交但**每步独立回执**（同构 `workflow_run` DAG——依赖执行、逐步结算，不合并掩盖中间失败）。
2. **幂等语义重试**：语义动作天然幂等可重试——`click(el)` 重试还是点它；程序负责重解析选择器，不要求模型记住「上次点到哪了」。
3. **失败语义化**：报错是语义事实（「提交按钮已禁用」「el_a3f2 已从 DOM 消失，相似候选: …」），不是渲染事实（「screenshot changed」「timeout」）。失败回执必须携带**模型下一步可用的信息**。
4. **乐观并发**：动作携带 `expected_state`/快照 version，元素状态与预期不符 → 拒绝执行 + 返回当前状态卡片（同构 `edit_file` 的 `expected_snapshot_ref` 乐观锁——防止模型基于过期解释操作）。
5. **等待显式化**：见动作契约③——等待条件语义化声明，程序轮询，超时回执携带超时时刻的状态 diff。
6. **回执即证据**：每次动作回执持久化为可回验记录（谁、何时、对哪个 ID、什么结果、绑定哪个快照版本）——操控历史可审计（同构 Evidence 持久化与 action_trace）。

## 五、程序的服务承诺（SLA）

程序作为服务方，六条硬承诺（违反任何一条 = 框架失信，比低效更严重）：

| # | 承诺 | 判据 |
|---|---|---|
| SLA-1 | ID 稳定与可水合 | 同一物理元素跨轮同 ID；任意 ID 可回验到接地快照 |
| SLA-2 | diff 忠实 | 报告的变化真实发生；未报告的变化不得静默发生 |
| SLA-3 | 动作原子性 | 要么完成且回执，要么失败且如实报告已发生的副作用 |
| SLA-4 | 回验通道常开 | 任意时刻可取原始接地（DOM 片段/截图），无场景禁用 |
| SLA-5 | 盲区与降级显式 | 不可达=标注；语义化失败=异常回执；永不静默空集 |
| SLA-6 | 硬边界最小且只做安全/授权/原子性/协议 | 对应 STACK L1 白名单；不越界做策略/完成裁决 |

**程序明确不做**：推荐元素、生成操作序列、判断任务完成、静默纠偏、静默重试、替模型丢弃语义卡片（裁剪是查询过滤，不是记忆删除）。

## 六、LFL 现行契约同构对照（本框架不是发明，是推广）

| LFL 已验证机制（现行 schema） | 语义操控框架对应物 |
|---|---|
| EvidenceRef + `read_evidence` 水合 | 语义 ID + 接地回验（②④） |
| `edit_file` 的 `snapshot_ref`/`expected_snapshot_ref` | 乐观并发动作 `expected_state`（四-4） |
| `source_synopsis`（摘要绑定原文 SHA，程序只存取） | 语义快照绑定接地版本；三级缩放（三-3） |
| `workflow_run` DAG（依赖执行、逐步回执） | 组合动作（四-1） |
| `architecture_status.context_usage` | 感知预算可见（三-6） |
| RULE-AI-01 事实诚实（先当前证据后结论） | 接地回验原则（P4） |
| RULE-AI-04 异常不静默 | 语义化失败不静默（SLA-5） |
| 方法总则①显式状态追踪 | diff 反馈 + 稳定 ID（模型不靠记忆页面） |

论证力：这些契约模式已在 LFL 文件/代码域内长期运行并被测试固化。本框架把它们**原样推广**到浏览器/OS 域——哲学同一，介质不同。

## 七、反模式清单（明确不做）

1. ❌ 程序推荐「最佳元素」——策略伪装成感知（违 P3）。
2. ❌ 全屏视觉语义化——成本反噬，语义化只花在结构信号盲区（违 P5）。
3. ❌ 静默降级到截图——降级必须显式回执（违 SLA-5）。
4. ❌ 程序自动重试/自动纠偏——决策权让渡（违 RULE-AI-00.2）。
5. ❌ 无 ID 的纯文本选择器每轮重新匹配——不稳定、不可回验（违 SLA-1）。
6. ❌ 把整屏机械无障碍树直接倒给模型——无任务裁剪，等于换了个格式的噪音（违 P5）。
7. ❌ 语义卡片携带「建议动作」字段——同 1。

## 八、分层架构与实施路线

```text
┌────────────────────────────────────────────────────────┐
│ L4 语义操控层（model）                                   │
│    模型在语义卡片/语义动作上思考与决策                    │
├────────────────────────────────────────────────────────┤
│ L3 任务裁剪层（program，查询过滤）                       │
│    按模型显式查询筛语义子集；预算可见；不做重要性判断     │
├────────────────────────────────────────────────────────┤
│ L2 语义化层（program 为主，vision 按需补盲）             │
│    DOM/树 → 语义卡片；ID 稳定追踪；diff 计算；            │
│    盲区标注；快照版本绑定                                │
├────────────────────────────────────────────────────────┤
│ L1 接地层（program，诚实兜底）                           │
│    原始 DOM/无障碍树/截图获取与持久化；硬安全边界；       │
│    原子执行（坐标解析、事件注入、系统调用）               │
└────────────────────────────────────────────────────────┘
信息流向：L1 采集 → L2 翻译 → L3 裁剪 → L4 决策；
动作流向：L4 语义动作 → L1 物理执行；
回验流向：L4 任意时刻 → L1 原始接地（跨层直达）。
```

- **Phase 0 契约规范**：先写工具 schema（感知/动作/反馈契约即工具定义），schema 即契约、即测试基线（LFL 方法：契约先行）。
- **Phase 1 浏览器域**：DOM 接地最完整、语义化规则化程度最高（role/name/state 大多可直接从 DOM 取），先在此验证四层与 SLA。Playwright/CDP 类驱动。
- **Phase 2 视觉补盲**：canvas/动态内容的按需 vision 语义化（模型显式请求才触发），验证「语义化只花在盲区」的成本边界。
- **Phase 3 OS 域**：Windows UIA / macOS AXAPI / Linux AT-SPI 分平台接地层实现；**语义层契约跨域不变**（这正是「程序是服务」的检验——换介质不换契约）。

## 九、开放问题（待用户裁决）

1. **组合动作的粒度上限**：批量提交允许多大步（5 步线性 vs 条件分支）？建议从纯线性起步。
2. **多标签页/多窗口的语义空间**：ID 全局唯一还是页面内唯一？影响水合寻址。
3. **OS 域的 Phase 3 优先级**：是否需要，还是浏览器域验证后再议。
4. **vision 补盲的成本闸门**：按请求次数、按 token 预算，还是按盲区面积？
5. **与 LFL 的关系定位**：独立框架（复用哲学），还是 LFL 的一个工具域扩展（复用 Evidence/回执基建）？这决定 Phase 0 的 schema 放在哪。

---
*规则引用均来自 `docs/ai_rules.md`（SoT）与 `docs/STACK.md` v0.1 草稿；LFL 工具契约引用来自本会话现行 schema（一手事实）。第三方效率数字未在本设计中断言。*
