# 最终分析与实施方案（三方综合：主区协调 × 镜像 LFL 执行 × 最终审查）

> 状态: **执行方案（定稿）** | 2026-08-20 | 协调: 主区（分析/审查）· 执行: 镜像 LFL · 依据: 三方交叉验证（镜像 LFL 评估 + codearts 复核 + 主区实证）

## 0. 结论基线（已验证事实，不再讨论）

1. **缓存架构成熟**：字节前缀缓存 + 稳定前缀 + 尾部追加友好；单模型长会话命中率 90%+ 实测。
2. **切换首轮全量 miss 是物理事实**（build→routing→452 时序），选 B 接受，不重构 routing。
3. **v2 切换通知已落地**（engine.py `_inject_switch_notice`），镜像 13 轮压力测试稳定（命中 85%→97%、任务 100% 记忆）。
4. **AI 主动维护上下文有真实实证**：镜像 LFL 在长会话测试中自主落盘+更新 `tasks/LONG-SESSION-TEST.md`（ALPHA/M1/77/7）——v3 落地依据成立。
5. **`ai_rules.lite.md`（2954 字符）已存在**，浓缩版就位；主文件（40157 字符）仍待 19 条移出。

## 1. 实施分工

| 角色 | 职责 |
|---|---|
| **主区（协调）** | 最终分析、方案定稿、实施后审查 |
| **镜像 LFL（执行）** | 按本方案实施 B 批 3 项，镜像验证，产出证据 |
| **主区（审查）** | 审查镜像实施结果，决定推广/提交 git |

## 2. 实施项（镜像 LFL 执行，按优先级）

### P0-1: EVO-a637d2d7 — 缓存命中率注入降级（已批）

**现状**：`config.py:269 cache_hit_show_in_answer=True` 每轮注入命中率 → 固定 token 开销 + 诱导 AI 复述尾巴（镜像测试反复见"⚡ 缓存命中率 N%"）。

**实施**：
- 默认值 `True → False`（或降级为"异常才提醒"：仅 `_cache_hint` 告警注入，常态 `format_health_note` 不注入）
- 保留"异常/切换"告警注入（那是 `_cache_hint` 分支，与常态注入分离）
- 期望：省每轮固定尾部 token + 根治 AI 复述尾巴

**验证**：镜像 chat 2 轮，确认 final_answer 不再带"⚡ 缓存命中率"尾巴；异常/切换提示仍正常。

### P0-2: M1 记忆分级 — 瞬时性内容 scope:session 过滤（已批）

**现状**：memory 500 条中 ~23%（115 条）为瞬时性内容（待核实），检索召回可能污染上下文。

**实施**：
- 核实 115/500 占比（`data/memory/` 扫描）
- 瞬时性条目（状态/临时/一次性）打 `scope:session` + 检索过滤（不参与全局召回）
- 保留永久性记忆（规则/经验/项目事实）

**验证**：检索召回质量对比（过滤前后命中相关度）；架构状态记忆计数。

### P1-3: EVO-0dbdf702 — 跨模型缓存共享实测（干净对照）

**现状**：切换轮命中 84% 受"池内其他会话前缀"干扰，无法定论。

**实施**（只读验证，不预设结论）：
- 方案 1（同 provider 跨模型）：全新会话 flash 建前缀 → 切 pro → 抓服务端 `prompt_cache_hit_tokens`
  - hit>0 → DeepSeek 池内跨模型共享 → 切换成本低于预期（优化锚点策略）
  - hit=0 → 按模型隔离 → 确认现状，关闭该项
- 方案 2（跨 provider）：deepseek → minimax 同理（预期必隔离，不同 API 端点）
- 抓取方式：切换轮不走 `_cache_hint` 分支时记录 usage 明细，或临时 spy

**验证**：两个方案的 cache hit 字段数据，得出"共享/隔离"结论。

### P2-1: 主文件 ai_rules.md 浓缩（19 条移出，需逐条 diff）

**现状**：`ai_rules.lite.md` 2954 字符已就位；主文件 40157 字符仍全量。

**实施**：按 ARCHITECTURE §8 逐条 diff（19 条规则 → 浓缩版），关键反例/数值保留；头部 version=N + 旧版作废声明。

**注意**：非零风险（13x 压缩），逐条核对后实施；此条可由镜像 LFL 做 diff 草案，主区审。

### P2-2: v3 declare_context 工具（有实证，可推进）

**现状**：镜像 AI 已实证"主动维护上下文"（LONG-SESSION-TEST.md 落盘+更新）。

**实施**（DESIGN-v3 §2）：
- 注册 `declare_context(recent=N, anchors=[轮号])` 工具（结构化参数）
- engine 452 切换后解析声明写锚点；未声明 → slim-first 兜底
- ai_rules.md 加一条规则（切换时 AI 应声明窗口）

**验证**：切换后 AI 声明窗口 → 锚点按声明写 → 命中率/任务接续对比。

## 3. git 提交建议（主区执行）

| 批 | 内容 | 时机 |
|---|---|---|
| A | engine.py + cache_health.py + client.py + 3 测试 + DESIGN 文档 | **现可提交**（镜像已验证稳定） |
| B | 上述实施项产物 | 实施验证后提交 |

## 4. 审查标准（主区最终审查）

- [ ] 每项实施的代码 diff 与方案一致，无越界改动
- [ ] 镜像验证证据（chat 实测 / 测试通过 / 数据）齐全
- [ ] 既有 151 测试无回归（镜像 PYTHONPATH 隔离验证）
- [ ] 两区代码一致（实施后同步）
- [ ] EVO-0dbdf702 有明确结论（共享/隔离），非悬置
- [ ] git 提交信息清晰（feature: xxx，附镜像验证摘要）
