# 任务 T1：统计 Markdown 文件数

你的工作目录（cwd）就是任务目录。目录树中有若干子目录，散布着扩展名不一的文件。

要求：递归统计整个目录树中 `*.md` 文件的**总数**，把**一个整数**（单独一行，无其他字符）写入当前目录下的 `report.txt`。

完成判据（客观判定）：
- `report.txt` 内容为一个整数，等于判定时刻目录树中实际存在的 `*.md` 文件总数；
- 原有的 `.md` 文件在判定时仍然全部存在（不允许靠删除文件凑数）。
