# 任务 T2：修复失败的测试

工作目录是一个小型 Python 项目：`calc.py`（实现）、`test_calc.py`（测试，**不许修改**）、`run_tests.sh`（测试入口）。

要求：
1. 运行测试，确认当前存在失败；
2. 定位并修复 `calc.py` 中的缺陷（以测试为准），**不得修改 `test_calc.py`**；
3. 重新运行测试，全部通过。

完成判据（客观判定）：判定时重新运行 `run_tests.sh` 退出码为 0，且 `test_calc.py` 内容与初始版本逐字节一致。
